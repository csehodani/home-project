CREATE TABLE birth_dates
(
    birth_date_id INT PRIMARY KEY NOT NULL AUTO_INCREMENT,
    day           INT             NOT NULL,
    month         INT             NOT NULL,
    year          INT             NOT NULL
);